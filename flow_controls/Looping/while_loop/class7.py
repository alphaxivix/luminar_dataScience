#lower limit and upper limit
# read a number from console , print elements from 1 to the number

lower = int(input("Enter the lower limit:"))
upper = int(input("Enter the upper limit:"))

while lower <= upper:
    print(lower)
    lower += 1